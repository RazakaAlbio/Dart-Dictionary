/* type system atau sound type system adalah sistem logis yang terdiri 
dari seperangkat aturan yang menetapkan properti atau tipe ke berbagai 
konstruksi program komputer, seperti variabel, expression, fungsi, atau modul. 
*/

/*type system generic adalah tanda <>, contoh pada list digunakan untuk mengklasifikasi

List<int> numberList = [1, 2, 3, 4, 5];
List<String> stringList = ['Dart', 'Flutter', 'Android', 'iOS'];
List dynamicList = [1, 2, 3, 'empat'];  // List<dynamic>

tipe parameter generic dilambangkan E, T, K, atau V.

contoh untuj mengurangi duplikasi code

abstract class Cache<T> {
  T getByKey(String key);
  void setByKey(String key, T value);
}
*/  
